import React, { Fragment } from 'react';

const LogicalOperator = () => (
    <Fragment>
        <section className='white-content'>
            <h4>Logical Operators</h4>

            <p>Logical operators allows us to add another layer of logic to our code.</p>
        </section>

        <section className='white-content'>
        <h4>Logical OR</h4>
    </section>

    <section className='white-content'>
        <h4>Logical AND</h4>
    </section>
    </Fragment>
);

export default LogicalOperator;